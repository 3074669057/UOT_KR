# Paper paragraph: fixed-delay leave-anchor-out audit

On the **fixed-delay** transport plan (`tx_if_available_else_flow_representative`), we repeat leave-key-out and leave-anchor-out-strict masking with admissible decoding evaluation. Strict masking removes bridge-derived evidence fields with **forbidden_features_remaining = 0**. Fake-oracle anchor injection is neutralized under strict masking. The fixed-delay RC-UOT-Q gains are not explained by bridge-key or bridge-evidence leakage.

Permuted-label controls collapse pair F1 to near zero across raw, top-3 rescue, and joint filter decodings. Legacy headline pair F1 ≈ 0.321 (pre-fixed-delay pipeline) is **not** used to support current claims.
