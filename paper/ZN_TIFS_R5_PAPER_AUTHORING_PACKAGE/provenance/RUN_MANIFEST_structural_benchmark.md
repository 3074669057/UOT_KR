# RUN_MANIFEST — faithful three-bridge split/merge structural recovery

Date: 2026-09-01 (Asia/Shanghai). Repo: `<REPO>`.

## Status

| bridge | feature provenance | status |
| --- | --- | --- |
| Celer | real flow segments from the frozen full pipeline + shared AML/evidence enrichment | PASS (regression anchor) |
| Multichain | real per-deposit flows; 7 wrapped assets priced via canonical maps (79% dev coverage: ETH/USDT/MCB/DAI/HGET/HYVE/DERC); AML/evidence/address real | PASS |
| PolyNetwork | real per-deposit flows; 11 assets priced (46% dev coverage); AML/evidence/address real; amounts lock==unlock by protocol | PASS |
| Connector-style / ABCTracer-style | existing structural baseline implementation | PASS (0.000 per seed, evaluator-computed) |

## Commands (reproduce)

```powershell
# 1. data artifacts (already committed under data/Token/):
#    multi_any_token_map.json  — Multichain any-token -> canonical map (provenance inside)
#    poly_eth_bsc_token_map.json — generated by:
python scripts/multi_bridge/gen_poly_token_map.py

# 2. feature pools (per bridge) + smoke (1 seed):
python scripts/multi_bridge/run_faithful_flow_structural.py --mode smoke --bridge Celer --rebuild-features
python scripts/multi_bridge/run_faithful_flow_structural.py --mode smoke --bridge Multi --rebuild-features
python scripts/multi_bridge/run_faithful_flow_structural.py --mode smoke --bridge Poly  --rebuild-features

# 3. full runs (48 templates x seeds 42-46 per bridge):
python scripts/multi_bridge/run_faithful_flow_structural.py --mode full --bridge Celer
python scripts/multi_bridge/run_faithful_flow_structural.py --mode full --bridge Multi --rebuild-features
python scripts/multi_bridge/run_faithful_flow_structural.py --mode full --bridge Poly  --rebuild-features

# 4. structural baselines (existing implementation, structure-level evaluator):
python scripts/multi_bridge/run_structural_three_bridges.py --out out/multi_bridge_expansion/faithful_flow_structural_three_bridges/baselines

# 5. flow-level one-to-one baselines through the SAME evaluator (transparency diagnostic):
python scripts/multi_bridge/run_one_to_one_flow_baselines.py --seeds 42

# 6. smoke-gate diagnostics:
python scripts/multi_bridge/run_smoke_gate_diagnostics.py

# 7. independent verification + authoritative aggregation:
python scripts/multi_bridge/verify_structural_results.py
```

## Parameters (frozen; identical for all bridges)

- UOT: reg=0.05, reg_m=0.5, lambda_risk=0.25, decode_threshold=1e-9,
  max_delay_sec=21600, causal_violation_penalty=5.0, backend=pot,
  allow_unmatched=True, use_graph_embedding=False, default cost weights
  (graph weight merged into amount via the frozen logic).
- Synthetic: 48 templates × seeds {42,43,44,45,46}; split = 1 src → 2 dst,
  merge = 2 src → 1 dst, plus unmatched and 2 decoys per template;
  decoy dst timestamps perturbed +60 s / +120 s (seed-controlled generator).

## Data paths

- Raw: `data/Validation/ETH-BNB/{Celer,Multi,Poly}/{input.csv,label.csv,sample.json}`
- Token metadata: `data/Token/{ERC20,BERC20}.csv` (decimals, NOT prices)
- USD prices: `data/Token/token_prices_usd.json` + canonical maps
  `data/Token/{multi_any_token_map,poly_eth_bsc_token_map}.json`
- ETH tx history (AML + corroboration): `data/FirstPhrase/{Celer,Multi,Poly}_ETH.csv`
- Cached expansion data: `out/multi_bridge_expansion/{Celer,Multi,Poly}/`
  (source_features.csv, development/test_candidates.csv, chronological_split_assignments.csv)
- Frozen Celer reference: `out/chapter4_repro_package/frozen_outputs/structural_recovery/synthetic_eval_aggregated.json`,
  `out/paper_full_pipeline_run/synthetic/synthetic_eval_seed_{42..46}/`
- Frozen Celer flow segments reused for the regression pool:
  `out/paper_full_pipeline_run/labels/flow_segments_{eth,bnb}.csv`

## Output paths

- `out/multi_bridge_expansion/faithful_flow_structural_three_bridges/`
  - `feature_stats/{bridge}/flow_segments_{eth,bnb}.csv`, `flow_labels.csv`, `feature_sanity.json`
  - `feature_stats/feature_sanity.{json,csv}`
  - `smoke/` (1-seed runs), `per_seed/{bridge}/seed_{s}/` (full artifacts: eval/, uot/, labels/, run_config.json)
  - `per_seed/structural_per_seed.csv`, `aggregated/structural_per_seed.csv`,
    `aggregated/structural_aggregated.csv`, `aggregated/verification_report.{json,md}`
  - `baselines/` (structural baseline evaluator outputs, per-seed CSV)
  - `diagnostics/` (diagnostics.{json,md}, smoke_gates.{json,md},
    one_to_one_flow_baselines_per_seed.csv)
  - `audit/feature_provenance.md`
  - `figures/`, `manuscript/`
  - `RUN_MANIFEST.md` (this file)

## Code changed (this task)

- `src/cross/domain/evaluation/semi_synthetic_flows.py` — decoy timestamp offsets in
  clone records (+60/+120 s); seed-controlled.
- `src/cross/domain/evaluation/synthetic_segment_subgraph.py` —
  `clone_segment_row(time_offset_sec=…)` applies real timestamp perturbation.
- `src/cross/domain/labels/uot_flow_loader.py` — reads optional `bridge` column
  (backward compatible).
- `scripts/multi_bridge/faithful_flow_features.py` (new) — real USD/AML/evidence/address
  feature builder for Multi/Poly + frozen-segment enrichment for Celer.
- `scripts/multi_bridge/run_faithful_flow_structural.py` (new) — orchestrator
  (smoke/full; frozen params).
- `scripts/multi_bridge/gen_poly_token_map.py` (new) — Poly ETH→BSC token map generator.
- `scripts/multi_bridge/run_structural_three_bridges.py` — `--out` flag + per-seed CSV
  export (baseline logic unchanged).
- `scripts/multi_bridge/run_smoke_gate_diagnostics.py`, `verify_structural_results.py`,
  `run_one_to_one_flow_baselines.py`, `export_feature_sanity_csv.py` (new diagnostics/
  verification).
- Data: `data/Token/multi_any_token_map.json` (new), `data/Token/poly_eth_bsc_token_map.json`
  (generated).
- Audit scripts under `scripts/multi_bridge/audit_*.py` + `diag_*.py` (provenance audits
  and bug diagnosis, kept for reference).

## Sensitive info

NodeReal RPC API keys in `config/local.json` / `config/local.runtime.json` were read
locally only (token symbol/decimals queries). No key appears in any artifact, log,
CSV, JSON, or markdown produced by this work.

## Final numbers (5 seeds, mean ± std)

| bridge | split | merge |
| --- | ---: | ---: |
| Celer | 0.950 ± 0.032 | 0.967 ± 0.043 |
| Multichain | 0.971 ± 0.035 | 0.979 ± 0.026 |
| PolyNetwork | 1.000 ± 0.000 | 1.000 ± 0.000 |
| Connector-style | 0.000 | 0.000 |
| ABCTracer-style | 0.000 | 0.000 |

Caveats: PolyNetwork saturates at 1.0 (documented in diagnostics.md §4 — its priced
templates carry no low-mass stress of the kind that produces Celer/Multichain's
per-seed misses). PolyNetwork amounts are lock==unlock by protocol (no fee asymmetry
in the decoded amounts).
